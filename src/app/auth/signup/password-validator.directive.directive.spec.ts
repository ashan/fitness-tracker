/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PasswordValidator.directiveDirective } from './password-validator.directive.directive';

describe('Directive: PasswordValidator.directive', () => {
  it('should create an instance', () => {
    const directive = new PasswordValidator.directiveDirective();
    expect(directive).toBeTruthy();
  });
});